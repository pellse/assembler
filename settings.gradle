pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "assembler"

include "cohereflux-core",
        "cohereflux-kotlin-extension",
        "cohereflux-cache-caffeine"
