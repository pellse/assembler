pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "cohereflux"

include "cohereflux-core",
        "cohereflux-kotlin-extension",
        "cohereflux-cache-caffeine"
