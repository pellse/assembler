pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "assembler"

include "assembler-util",
        "reactive-assembler-core",
        "reactive-assembler-kotlin-extension",
        "reactive-assembler-cache-caffeine"
