pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "assembler"

include "assembler",
        "assembler-kotlin-extension",
        "assembler-cache-caffeine"
